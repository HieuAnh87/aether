# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Downloaded sidecar binaries (real CLIProxyAPI binaries are large, download via scripts/download-sidecar.sh)
# Keep the placeholder for dev reference, ignore real binaries (>1KB)
/binaries/cliproxyapi-*
!/binaries/.gitkeep
